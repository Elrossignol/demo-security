name: Exemple cicd
on:
    push:
        branches: [main]

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: step 1
              run: echo "Hello world !!"
            - name: step 2
              uses: actions/checkout@v7
            - name: step 3
              run: ls -l -a